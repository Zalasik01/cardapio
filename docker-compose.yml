services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: cardapio
      POSTGRES_USER: cardapio
      POSTGRES_PASSWORD: cardapio
    ports:
      - "5432:5432"
    volumes:
      - cardapio_postgres_data:/var/lib/postgresql/data

volumes:
  cardapio_postgres_data:
