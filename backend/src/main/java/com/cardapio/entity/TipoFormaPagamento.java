package com.cardapio.entity;

/** Tipo da forma de pagamento (define o comportamento e o ícone; o nome é livre). */
public enum TipoFormaPagamento {
    DINHEIRO,
    PIX,
    CARTAO_CREDITO,
    CARTAO_DEBITO,
    VALE_REFEICAO,
    OUTRO
}
